/**
 * Note for selecion buttons
 */
export const selectionNote = `(here is the text market. If a user presses red, he is able
highlight the words in the text below in red)`;

/**
 * Note for filter buttons
 */
export const filterNote = `(here is the filter. If a user presses on red, all red text
should appear in the box below)`;
